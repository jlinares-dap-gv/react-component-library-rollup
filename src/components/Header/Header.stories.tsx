import React from "react";
import Header from './Header';

export default {
  title: "Header"
};

export const Primary = () => <Header theme="primary" />;

export const Secondary = () => <Header theme="secondary" />;